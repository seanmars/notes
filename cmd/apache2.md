**check which apache modules are enabled/installed**

    apache2ctl -M

**some command**

    // status
    // start
    // stop
    // restart
    /etc/init.d/apache2 xxxx
    service apache2 xxxx
    
